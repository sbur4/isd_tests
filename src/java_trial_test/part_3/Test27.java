package java_trial_test.part_3;

/**
 * 27. Which are true about a static nested class? (Choose all that apply)
 * <p>
 * A. You must have a reference to an instance of the enclosing class in order  to instantiate it.
 * B. It does not have access to non-static members of the enclosing class.+
 * C. Its variables and methods must be static.
 * D. If the outer class is named MyOuter, and the nested class is named MyInner, it can be instantiated using
 * new MyOuter.MyInner();.
 * E. It must extend the enclosing class.
 */

// B + C + D