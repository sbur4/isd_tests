package java_trial_test.part_5;

/**
 * 39. Which are true? (Choose all that apply.)
 * <p>
 * A. The notifyAll() method must be called from a synchronized context. +
 * B. To call wait(), an object must own the lock on the thread. +
 * C. The notify() method is defined in class java.lang.Thread. +
 * D. When a thread is waiting as a result of wait(), it release its lock.
 * E. The notify() method causes a thread to immediately release its lock. +
 * F. The difference between notify() and notifyAll() is that notifyAll() notifies all waiting threads, regardless of the
 * object they're waiting on. +
 */

// A + B + C + E + F