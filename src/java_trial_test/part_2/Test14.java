package java_trial_test.part_2;

/**
 * 14. What is in your opinion recommended rule of using checked and unchecked exceptions?
 * <p>
 * A.One should use unchecked exception only if he/she doesn’t want someone to catch it.
 * B.There is no rules. It is up to developer whenever to use in particular situation checked or unchecked exception
 * C.In general one should use unchecked exception in case of unexpected, non-recoverable emergency situation, and
 * checked in case of expected. +
 * D.One shouldn't use checked exceptions at all, if possible, because checked exception force you to mention it in a
 * throw clause.
 */

// C